export type RegisterHealthInfoCardProps = {
  title: string;
  description?: string;
  backgroundColor?: string;
  borderColor?: string;
  textColor?: string;
  iconColor?: string;
  onPress?: () => void;
};

